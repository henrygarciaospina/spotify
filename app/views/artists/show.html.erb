<div class="container"> 
  <div class="row">
    <div class="col-sm-7">
      <div class="row">
        <% @artist.albums.each do |album| %>
          <div class="col-sm-3">
            <div class="image-responsive thumbnail" align="center">
            <%= image_tag(album.image_url, :class => "photo") %>
            <h3><%= album.name %></h3>
          </div>
          </div>
        <% end %>  
      </div>
    </div>
    <div class="col-sm-5">
      
    </div>
  </div>
</div>